const catchAsync = require('../../utils/catchAsync');
// const { {{camelCase name}}Service } = require('../../services');

const create{{pascal name}} = catchAsync(async (req, res) => {
// const user = await {{camelCase name}}Service.create{{camelCase
name}}(req.body);
// res.send({ user });
});

module.exports = {
create{{pascal name}},
};
